<div class="well">
  <%== form_tag "/admin/types/#{@edit_type_id}" do %>
  <table class="table">
    <thead>
      <tr>
        <th>Type Name</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%== text_field_tag :name, @name, size: 40 %></td>
      </tr>
  </table>
  <% if params[:id] != "new" %>
    <%== submit_tag "Update Type" %> <%== submit_tag "Delete Type" %>
  <% else %>
    <%== submit_tag "Add Type" %>
  <% end %>
</tbody>
  <% end %> <!-- This end is for the form tag. -->
</div>
<div class="well">
  <table class="table table-striped table-hover table-bordered table-fixed">
  <thead>
    <tr>
      <th></th>
      <th>Name</th>
    </tr>
  </thead>
  <tbody>
    <% @types.each do |type| %>
    <tr>
      <td><a href="/admin/types/<%= type.id %>">Edit</a></td>
      <td><%= type.name %></td>
    </tr>
    <% end %>
  </tbody>
</table>
</div>