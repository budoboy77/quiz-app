<div class="container">
  <div class="well">
    <h4>Quizzes assigned to me:</h4>
    <% @my_quizzes.each do |quiz| %>
    <a href="/quiz/<%= quiz.id %>"><%= quiz.name %></a>
    <% end %>
  </div>
</div>